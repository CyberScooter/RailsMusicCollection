<div class="container">
    <div class="jumbotron">
        <h3>New Song</h3>
        <p class="errorMessage"> <%= notice %> </p>
        <%= render 'form', song: @song %>

        <%= link_to 'Back', songs_path, class:"btn btn-default btn-light" %>
    </div>
</div>
